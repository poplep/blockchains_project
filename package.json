{
	"name": "blockchains",
	"version": "1.0.0",
	"description": "Project that lets users interact with the blockchain in a UI.",
	"main": "truffle.js",
	"directories": {
	  "test": "test"
	},
	"scripts": {
	  "dev": "lite-server",
	  "test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Me",
	"license": "ISC",
	"devDependencies": {
		"lite-server": "^2.3.0",
		"web3": "^0.19.1"
	}
  }